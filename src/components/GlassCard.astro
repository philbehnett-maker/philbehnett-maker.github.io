---
interface Props {
  class?: string;
  'data-animate'?: string;
}

const { class: className = '', ...rest } = Astro.props;
---

<div class:list={['glass-card rounded-2xl', className]} {...rest}>
  <slot />
</div>
